# ServiceMonitor for Prometheus Operator
# Uncomment to enable Prometheus scraping via ServiceMonitor
#
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: {{ .Values.name }}
#   labels:
#     app: {{ .Values.name }}
# spec:
#   selector:
#     matchLabels:
#       app: {{ .Values.name }}
#   endpoints:
#   - port: metrics
#     path: /metrics
#     interval: 30s
